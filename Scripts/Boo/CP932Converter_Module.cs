using System;
using System.Runtime.CompilerServices;
using System.Text;

[CompilerGlobalScope]
public sealed class CP932Converter_Module
{
	private CP932Converter_Module()
	{
	}

	private static void Main(string[] argv)
	{
		if (typeof(UTF8Encoding) as UTF8Encoding is IDisposable disposable)
		{
			disposable.Dispose();
			IDisposable disposable2 = null;
		}
		if (typeof(UnicodeEncoding) as UnicodeEncoding is IDisposable disposable3)
		{
			disposable3.Dispose();
			IDisposable disposable4 = null;
		}
	}
}
